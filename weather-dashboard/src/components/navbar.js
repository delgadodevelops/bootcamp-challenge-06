import React from "react";

const navbar = () => {
  return (
    <header>
      <nav className="bg-blue-200 font-archivo text-center justify-between items-center p-5">
        <a className="text-xl font-semibold" href="#">
          Weather Dashboard
        </a>
      </nav>
    </header>
  );
};

export default navbar;
